{
  "name": "aila-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/desktop dev",
    "cli": "bun run --cwd apps/cli start",
    "tui": "bun run --cwd apps/tui start",
    "build:packages": "tsc -b packages/agent packages/agent-node",
    "build": "bun run build:packages && bun run --cwd apps/desktop build",
    "icons": "node scripts/generate-icons.mjs",
    "typecheck:agent": "bun run --cwd packages/agent typecheck",
    "typecheck:agent-node": "bun run --cwd packages/agent-node typecheck",
    "typecheck:agent-package": "tsc --noEmit -p tsconfig.runtime-package.json --composite false",
    "typecheck:desktop": "bun run --cwd apps/desktop typecheck",
    "typecheck:cli": "bun run --cwd apps/cli typecheck",
    "typecheck:tui": "bun run --cwd apps/tui typecheck",
    "typecheck": "bun run typecheck:agent && bun run typecheck:agent-node && bun run typecheck:agent-package && bun run typecheck:desktop && bun run typecheck:cli && bun run typecheck:tui",
    "postinstall": "bun run build:packages && bun run --cwd apps/desktop postinstall",
    "build:mac": "bun run build && bun run --cwd apps/desktop build:mac",
    "build:win": "bun run build && bun run --cwd apps/desktop build:win",
    "build:linux": "bun run build && bun run --cwd apps/desktop build:linux",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "format": "biome format --write .",
    "test:architecture": "bun scripts/package-boundaries-contract.ts",
    "test:agent": "bun scripts/agent-contract.ts",
    "test:runtime": "bun scripts/runtime-contract.ts",
    "test:desktop": "bun scripts/desktop-workbench-contract.ts",
    "test:cli": "bun scripts/cli-contract.ts",
    "test:tui": "bun scripts/tui-contract.ts",
    "test": "bun run build:packages && bun run test:architecture && bun run test:agent && bun run test:runtime && bun run test:desktop && bun run test:cli && bun run test:tui"
  },
  "devDependencies": {
    "@aila/agent": "workspace:*",
    "@aila/agent-node": "workspace:*",
    "@biomejs/biome": "^2.4.10",
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
